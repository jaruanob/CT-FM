CONSTANTS#version: "lr=dynamic_global_grid_size=[8,16,16]_SegResNetDS"
CONSTANTS#HIGH_RES_GRID_SIZE: [8, 16, 16]

system#model#num_ftrs: 512
system#model#backbone: 
    _target_: project.models.segresnetds_w_embedding.SegResNetDSwEmbedding
    spatial_dims: 3
    in_channels: 1
    out_channels: 1  # Doesn't matter, not training the decoder
    init_filters: 32
    blocks_down: [1, 2, 2, 4, 4]